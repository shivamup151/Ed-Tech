services:
  - type: web
    name: ed-tech
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    aptPackages:
      - libreoffice
      - imagemagick
      - poppler-utils
